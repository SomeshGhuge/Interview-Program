package Abstraction;

public abstract class Abstract_class {// u must have to put abstract keyword in between public and class.
	abstract void mo();// This is abstract method
	//it is necessary that abstraction is 0-100% in abstraction
	void na() {
		//concrete methods here
	}
	
	
	//info------>
	//Abstract class hve constructor
	//in abstract class we achive 0 to 100% abstraction
	//it contains abstract methods or concrete methods or empty class or combination of both
	//to use abstract method class,we should extends the abstract class and use that methods
	// if u dont want to implement or override that method ,make those methods as abstract.
	// if any method inclass is abstract them that class must be decleared that abstract.
// abstraction also known as aggregation and composition.
}
