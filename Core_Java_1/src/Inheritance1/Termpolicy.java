package Inheritance1;

public class Termpolicy extends Policy {
	//it is a sub class/child class/derived class/new class
	
	//A child class cant extends to multipe parent classes
	//Inherit the classes by using extends keyword
	//whenever we creat a object in subclass then all members which are in it are called as the super class as well as sub class
	//
	// we use the inheritance because we want use code reusable
	//reusable means we can use the existing class feature such as methods,variables,objects etc
	//

}
